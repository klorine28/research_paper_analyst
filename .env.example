# Copy to .env (never commit .env). Document every new variable here with its
# default and why it exists; see CODING_STANDARDS.md > Configuration.
#
# Expected once open decisions in docs/BRIEF.md are resolved, e.g.:
# LLM provider API key (extraction / narrative text)
# Scholarly API contact email or key (corpus ingestion)
